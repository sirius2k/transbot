# ============================================================================
# TransBot 환경 변수 설정 파일 예시
# ============================================================================
#
# 이 파일을 복사하여 `.env` 파일을 생성하고 값을 수정하세요:
#   cp .env.example .env
#
# 주의: `.env` 파일은 민감한 정보(API 키)를 포함하므로 Git에 커밋하지 마세요.
#

# ============================================================================
# OpenAI API 설정
# ============================================================================

# OpenAI API 키 (필수)
# OpenAI 대시보드(https://platform.openai.com/api-keys)에서 발급받은 API 키를 입력하세요.
OPENAI_API_KEY=your_openai_api_key_here

# API 타임아웃 (초 단위)
# OpenAI API 호출 시 최대 대기 시간입니다.
# 기본값: 60초
# OPENAI_API_TIMEOUT=60

# API 재시도 횟수
# API 호출 실패 시 재시도할 최대 횟수입니다.
# 기본값: 3회
# OPENAI_MAX_RETRIES=3

# ============================================================================
# AI 모델 설정
# ============================================================================

# 기본 AI 모델
# 번역에 사용할 기본 AI 모델을 지정합니다.
# 지원 모델: gpt-4o, gpt-4o-mini, gpt-4-turbo, gpt-4, gpt-3.5-turbo
# 권장: gpt-4o-mini (가성비 우수, 번역 품질 높음)
# 기본값: gpt-4o-mini
# DEFAULT_MODEL=gpt-4o-mini

# 번역 창의성 설정 (Temperature)
# 0.0에서 1.0 사이의 값으로, 낮을수록 일관성 있고 높을수록 창의적입니다.
# 번역 작업에는 낮은 값(0.3)이 권장됩니다.
# 기본값: 0.3
# DEFAULT_TEMPERATURE=0.3

# 최대 출력 토큰 수
# AI가 생성할 수 있는 최대 토큰 수입니다.
# 긴 텍스트 번역 시 이 값을 늘릴 수 있습니다.
# 기본값: 4000
# MAX_TOKENS=4000

# OpenAI 모델 필터링 (선택사항)
# UI에 표시할 OpenAI 모델을 제한합니다.
# 쉼표로 구분된 모델 목록을 입력하세요.
# 설정하지 않으면 모든 지원 모델이 표시됩니다.
# 예: gpt-4o,gpt-4o-mini (두 모델만 표시)
# 사용 사례: API 키가 특정 모델만 지원하는 경우
# OPENAI_MODELS=gpt-4o,gpt-4o-mini

# ============================================================================
# 언어 감지 설정
# ============================================================================

# 한국어 감지 임계값
# 텍스트에서 한국어로 판단할 최소 비율입니다. (0.0 ~ 1.0)
# 예: 0.5 = 전체 문자 중 50% 이상이 한국어일 때 "Korean"으로 감지
# 기본값: 0.5
# LANGUAGE_DETECTION_THRESHOLD=0.5

# ============================================================================
# 애플리케이션 설정
# ============================================================================

# 페이지 제목
# 브라우저 탭에 표시되는 제목입니다.
# 기본값: 영어-한국어 번역기
# APP_TITLE=TransBot

# 페이지 아이콘 (Favicon)
# 브라우저 탭에 표시되는 이모지 또는 아이콘입니다.
# 기본값: 🌐
# APP_ICON=🌐

# 레이아웃 모드
# Streamlit 페이지의 레이아웃 모드를 지정합니다.
# 옵션: centered (가운데 정렬), wide (전체 너비)
# 기본값: centered
# APP_LAYOUT=centered

# ============================================================================
# UI 설정
# ============================================================================

# 텍스트 영역 높이 (픽셀)
# 번역 입력 텍스트 영역의 높이를 지정합니다.
# 기본값: 200
# TEXT_AREA_HEIGHT=200

# 최대 입력 길이 (문자 수)
# 한 번에 번역할 수 있는 최대 문자 수입니다.
# 이 값을 초과하면 경고 메시지가 표시됩니다.
# 기본값: 50000
# MAX_INPUT_LENGTH=50000

# ============================================================================
# Azure OpenAI 설정
# ============================================================================
#
# Azure OpenAI Service를 사용할 경우 아래 설정을 구성하세요.
# OpenAI와 Azure 중 하나만 선택하여 사용할 수 있습니다.

# AI Provider 선택
# OpenAI 또는 Azure OpenAI Service 중 선택합니다.
# 옵션: openai, azure
# 기본값: openai
# AI_PROVIDER=openai

# Azure OpenAI API 키
# Azure Portal의 "Keys and Endpoint" 섹션에서 확인할 수 있습니다.
# Azure를 사용할 경우 필수입니다.
# AZURE_OPENAI_API_KEY=your_azure_api_key_here

# Azure OpenAI Endpoint
# Azure Portal의 "Keys and Endpoint" 섹션에서 확인할 수 있습니다.
# 예: https://your-resource-name.openai.azure.com/
# Azure를 사용할 경우 필수입니다.
# AZURE_OPENAI_ENDPOINT=https://your-resource-name.openai.azure.com/

# Azure OpenAI API 버전
# Azure OpenAI API의 버전을 지정합니다.
# 최신 버전은 Azure 문서에서 확인하세요.
# 기본값: 2024-02-15-preview
# AZURE_OPENAI_API_VERSION=2024-02-15-preview

# Azure Deployment 매핑
# Azure에서 생성한 deployment 이름을 모델명과 매핑합니다.
# 형식: "모델명:deployment명,모델명:deployment명"
# 예: gpt-4o:my-gpt4o-deployment,gpt-4o-mini:my-mini-deployment
# AZURE_DEPLOYMENTS=gpt-4o:my-gpt4o,gpt-4o-mini:my-mini

# ============================================================================
# Langfuse 설정 (LLM 관찰성)
# ============================================================================
#
# Langfuse를 사용하여 LLM 사용 내역을 추적하고 비용 분석, 품질 모니터링을 수행할 수 있습니다.
# 자체 호스팅 Langfuse 인스턴스를 사용하며, 설정하지 않으면 추적이 비활성화됩니다.

# Langfuse Public Key
# Langfuse 대시보드의 Settings > API Keys에서 발급받은 Public Key를 입력하세요.
# 선택사항: 설정하지 않으면 Langfuse 추적이 비활성화됩니다.
# LANGFUSE_PUBLIC_KEY=pk-lf-your-public-key-here

# Langfuse Secret Key
# Langfuse 대시보드의 Settings > API Keys에서 발급받은 Secret Key를 입력하세요.
# 선택사항: 설정하지 않으면 Langfuse 추적이 비활성화됩니다.
# LANGFUSE_SECRET_KEY=sk-lf-your-secret-key-here

# Langfuse Host URL
# 자체 호스팅 Langfuse 인스턴스의 URL을 입력하세요.
# 예: http://localhost:3000 (로컬 개발), https://langfuse.yourdomain.com (프로덕션)
# 선택사항: 설정하지 않으면 Langfuse 추적이 비활성화됩니다.
# LANGFUSE_HOST=http://localhost:3000

# ============================================================================
# 참고 사항
# ============================================================================
#
# 1. 환경 변수 적용 방법:
#    - 위의 주석 처리된 설정을 사용하려면 앞의 `#` 기호를 제거하세요.
#    - 예: # DEFAULT_MODEL=gpt-4o-mini → DEFAULT_MODEL=gpt-4o
#
# 2. 환경 변수 미설정 시:
#    - 위에 명시된 기본값이 자동으로 사용됩니다.
#    - OPENAI_API_KEY만 반드시 설정하면 나머지는 선택사항입니다.
#
# 3. 설정 적용 확인:
#    - Streamlit 앱을 재시작하면 새로운 설정이 적용됩니다.
#    - `python3 -c "from config import Config; c = Config.load(); print(c.DEFAULT_MODEL)"`
#
# 4. 보안:
#    - `.env` 파일은 절대 Git에 커밋하지 마세요. (.gitignore에 포함됨)
#    - API 키는 외부에 노출되지 않도록 주의하세요.
#
