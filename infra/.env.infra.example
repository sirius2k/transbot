# TransBot Infrastructure Environment Variables
# Copy this file to .env.infra and update with your values

# =============================================================================
# PostgreSQL Configuration
# =============================================================================
POSTGRES_DB=transbot
POSTGRES_USER=transbot_user
# 로컬 개발용 기본값 (프로덕션에서는 반드시 변경하세요!)
POSTGRES_PASSWORD=transbot_password
POSTGRES_PORT=5432

# =============================================================================
# ClickHouse Configuration (Required by Langfuse v3)
# =============================================================================
CLICKHOUSE_PORT=8123
CLICKHOUSE_DB=langfuse
CLICKHOUSE_USER=langfuse
# 로컬 개발용 기본값 (프로덕션에서는 반드시 변경하세요!)
CLICKHOUSE_PASSWORD=langfuse_password

# =============================================================================
# Langfuse Configuration (LLM Observability)
# =============================================================================
LANGFUSE_URL=http://localhost:3000
LANGFUSE_PORT=3000
LANGFUSE_SECRET=change-me-to-random-string-at-least-32-chars
LANGFUSE_SALT=change-me-to-another-random-string

# Langfuse API Keys (생성 후 TransBot .env에 추가)
# LANGFUSE_PUBLIC_KEY=pk-lf-xxx
# LANGFUSE_SECRET_KEY=sk-lf-xxx

# =============================================================================
# Redis Configuration
# =============================================================================
REDIS_PORT=6379
# 로컬 개발용 기본값 (프로덕션에서는 반드시 변경하세요!)
REDIS_PASSWORD=redis_password

# =============================================================================
# Development Options
# =============================================================================
# 로그 레벨: debug, info, warn, error
LOG_LEVEL=info
